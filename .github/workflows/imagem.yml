name: Imagem

# A cada versão (`git tag v0.3.0 && git push --tags`), a imagem sobe para o
# GitHub Container Registry com a etiqueta da versão e `latest`. É a imagem que
# o docker-compose.yml puxa em quem sobe a própria instância sem construir.

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publicar:
    name: construir e publicar
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/gabrielmello1407/rotamer
          # Etiqueta `v0.3.0` vira `0.3.0`, `0.3` e `latest`. Uma execução manual
          # fora de etiqueta sobe só com o hash do commit, sem tocar em `latest`.
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=sha,enable=${{ !startsWith(github.ref, 'refs/tags/v') }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
