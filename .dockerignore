# O que não entra no contexto de construção da imagem. A regra é a mesma do
# `.gitignore`, mais o que só serve a quem desenvolve.

node_modules
**/node_modules
.pnpm-store

.next
**/.next
dist
out
build
*.tsbuildinfo
.turbo
coverage
playwright-report
test-results

# Gerado no `prebuild` e no `postinstall`, dentro da própria imagem.
apps/web/public/chem
apps/web/generated
apps/web/next-env.d.ts

# Segredo nunca entra na imagem — nem o `.env` da raiz, nem o de `apps/web`,
# que o `next build` copiaria para dentro da saída standalone. Padrão sem
# `**/` só casa na raiz do contexto.
**/.env
**/.env.*
!**/.env.example

.git
.github
.claude
.codex
.vscode
docs
brand
scripts
*.md
