# workspace: pacotes internos sempre resolvidos do próprio repositório
link-workspace-packages=true
# hoisting explícito: dependência não declarada não pode ser importada por acidente
strict-peer-dependencies=false
